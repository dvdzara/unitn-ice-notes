:3000 {
    root * /srv
    file_server
    encode zstd gzip

    handle_errors 404 {
        rewrite * /404.html
        file_server
    }

    handle_errors {
        respond "{err.status_code}: {err.status_text}"
    }
}