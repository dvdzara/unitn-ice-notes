---
sidebar:
  order: 0
next: false
prev: false
tableOfContents: false
title: Information and Computer Engineering course notes
---

import { getCollection } from "astro:content";

These notes should resume lessons taken at UniTn's ICE bachelor course.

## Courses

<ul>
  {(await getCollection("docs"))
    .filter((c) => c.data.sidebar.order == 1)
    .map((c) => (
      <li>
        <a href={`/${c.slug}`}>{c.data.title}</a>
      </li>
    ))}
</ul>

## Contribute

Open a merge request with your changes on this Gitlab
[repository](https://git.zarantonello.dev/davide/ice-notes).
