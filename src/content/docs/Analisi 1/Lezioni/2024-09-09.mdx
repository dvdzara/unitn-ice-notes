---
title: Lezione 1 (2024-09-09)
---

import { Details } from "../../../../components";
import { LinkCard, Card, CardGrid } from "@astrojs/starlight/components";

## Insiemi numerici

- non importa l'ordine degli elementi;
- composti da simboli, numeri o altri insiemi;

<details>
<summary>Esempi</summary>

- $$A = \{ a,b,c,d,e \}$$;
- $$B = \{ \text{proprietà che accomuna gli elementi} \}$$;
- $$C = \{ 2k+1 \| k=0,1,2,\ldots \}$$;

</details>

Possono essere definiti come elenco ($$A$$) o indicando la proprietà che
accomuna tutti gli elementi ($$B$$).

- $$a \in A$$ ($$a$$ appartiene ad $$A$$);
- $$a \notin A$$ ($$a$$ non appartiene ad $$A$$);

Simbolo dell'insieme vuoto: $$\emptyset$$

### Operazioni tra insiemi

- unione: $$A \cup B = \{ x \| x \in A \OR x \in B \}$$;
- intersezione: $$A \cap B = \{x \| x \in A \AND x \in B \}$$;
- differenza: $$A \setminus B = \{ x \| x \in A \AND x \notin B \}$$;
- prodotto cartesiano: $$A \times B = \{ (a,b) \| a \in A \C b \in B \}$$;

### Sottoinsieme

$$A$$ è sottoinsieme di $$B$$ solo se tutti gli elementi di $$A$$ sono anche in
$$B$$.

Simbolo di sottoinsieme: $$A \subseteq B$$

Sottoinsieme strettamente contenuto: $$A \subsetneqq B$$,
$$\EX x \| x \in B \AND x \notin A$$.

:::note

- $$A \cup B$$ è sottoinsieme di $$A$$ e $$B$$;
- $$A \cap B$$ non è sottoinsieme nè di $$A$$, nè di $$B$$;
- $$A \subseteq B \AND B \subseteq A \ARR A = B$$;

:::

## Linguaggio matematico

### Proposizioni

Rappresentano qualcosa che può essere vero o falso.

**Esempio**: $$a \in A$$

### Operatori logici ($$\AND$$, $$\OR$$, $$\NOT$$)

| $$P$$ | $$Q$$ | $$\AND$$ | $$\OR$$ | $$\NOT P$$ |
| ----- | ----- | -------- | ------- | ---------- |
| v     | v     | v        | v       | f          |
| v     | f     | f        | v       | f          |
| f     | v     | f        | v       | v          |
| f     | f     | f        | f       | v          |

---

Leggi di De Morgan (negazioni degli operatori logici):

- $$\NOT \( A \AND B \) = \( \NOT A \) \OR \( \NOT B \)$$;
- $$\NOT \( A \OR B \) = \( \NOT A \) \AND \( \NOT B \)$$;

### Predicato

Proposizione con variabili.

**Esempio**: $$P(x) \| x \text{ è pari}$$

Il risultato può dipendere o no dal valore della variabile.

### Implicazione e doppia implicazione

- implicazione: $$P \ARR Q$$ (se $$P$$ allora $$Q$$, $$Q$$ solo se $$P$$);
- doppia implicazione: $$P \ALRR Q$$ ($$P$$ se e solo se $$Q$$);

| $$P$$ | $$Q$$ | $$P \ARR Q$$ | $$P \ALRR Q$$ |
| ----- | ----- | ------------ | ------------- |
| v     | v     | v            | v             |
| v     | f     | f            | f             |
| f     | v     | v            | f             |
| f     | f     | v            | v             |

Nell'implicazione, se la prima proposizione è falsa, il risultato della seconda
non ha importanza.

$$P \ALRR Q = \( P \ARR Q \) \AND \( Q \ARR P \)$$

Negazione di una proposizione con implicazioni:
$$P \ARR Q \equiv \( \NOT Q \) \ARR \( \NOT P \)$$

### Quantificatori

- esistenziale: $$\EX$$

  **Esempio**: $$\EX a \in A$$: esiste $$a$$ appartenente ad $$A$$

- universale: $$\forall$$

  **Esempio**: $$\forall a \in A$$: ogni $$a$$ appartiene ad $$A$$

Negazione di una proposizione con quantificatori:

- $$\NOT \( \forall x \C P(x) \) = \EX x \| \NOT P(x)$$;
- $$\NOT \( \EX x \| P(x) \) = \forall x \C \NOT P(x)$$;

:::note

Con $$\forall$$ non si usa il simbolo $$\vert$$ (tale che): si indica soltanto
la proprietà che è soddisfatta da tutti gli elementi.

:::

---

<CardGrid>
  <LinkCard
    title="Appunti di Andrea"
    href="https://drive.google.com/file/d/1bNz9K4KZHSPo4fzZx6TqLPEF4KxXE9m_/view"
  />
</CardGrid>
